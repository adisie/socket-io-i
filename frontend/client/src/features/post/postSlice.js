import {createSlice} from '@reduxjs/toolkit'


// initial state
const initialState = {

}

const postSlice = createSlice({
    name: 'post',
    initialState,
    reducers: {

    },
    extraReducers: build => {

    },
})

export default postSlice.reducer